Mono overline label that introduces a heading — the brand's "coordinate" voice.

```jsx
<Kicker index="01" tick>The Asia-Connect Advantage</Kicker>
```

Props: `index` (leading number), `tick` (brass rule), `accent` (all-brass).
