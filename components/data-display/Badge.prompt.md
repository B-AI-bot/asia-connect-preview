Compact uppercase marker for status or category (e.g. "Engagement live", "Singapore HQ").

```jsx
<Badge tone="success" dot>Active mandate</Badge>
<Badge tone="brass">Interim</Badge>
```

Tones: `neutral` / `brass` / `success` / `warning` / `danger`. Set `solid` for a filled badge, `dot` for a status dot.
